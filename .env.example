# When set, all /data/* fetches are redirected to this base URL.
# Point it at your Cloudflare R2 bucket public URL (without trailing slash).
# Example: https://data.yourdomain.com/data
# Leave unset for local dev — requests fall through to public/data/
NEXT_PUBLIC_DATA_BASE_URL=
